# Setup Guide

1. Install Docker and Docker Compose.
2. Copy env file: `cp backend/.env.example backend/.env`.
3. Start platform: `./scripts/bootstrap.sh`.
4. Open backend docs and frontend dashboard.
